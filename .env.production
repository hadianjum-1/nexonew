# Production environment — committed to git (no secrets here, only public URLs)
# Vite reads this file automatically when building for production (npm run build)
VITE_API_URL=https://contactform-nqte.onrender.com
